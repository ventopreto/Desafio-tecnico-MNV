  <% if @users.empty? %>
    <p><%= t(".no_user") %></p>
  <% else %>
    <h5><%= t(".title") %> </h5>
  <% @users.each do |user| %>
<div class="card rounded-circle border border-dark" style="width: 18rem;">
</div>
  <div class="card-body">
    <h5><%= "#{t(".name")}: " %> <%= link_to user.name, user_path(user) %> </h5>
  </div>
 <% end %>
<% end %>
<%= link_to t("links.create"), new_user_path, class: "btn btn-outline-dark btn-sm" %>
<%= link_to t("links.back"), root_path, class: "btn btn-outline-dark btn-sm" %>
